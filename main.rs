mod free_throw_monte_carlo;
use free_throw_monte_carlo::*;

fn main() {
    // println!(
    //     "the mean sum of free throws required to hit {} free throws with {} accuracy is {}",
    //     SHOTS_TO_MAKE_CONSEQUTIVELY,
    //     FREE_THROW_PROP,
    //     mean(monte_carlo(100))
    );
}
